<%= form_for(@friend_pool) do |f| %>
  <% if @friend_pool.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@friend_pool.errors.count, "error") %> prohibited this friend_pool from being saved:</h2>

      <ul>
      <% @friend_pool.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
